{
  "name": "@neftie/subgraph",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "rimraf -rf build generated",
    "codegen": "yarn clean && graph codegen",
    "build": "graph build",
    "predeploy": "yarn codegen",
    "deploy:goerli": "yarn predeploy && graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ neftielabs/neftie-rinkeby ./subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "devDependencies": {
    "rimraf": "3.0.2"
  }
}
