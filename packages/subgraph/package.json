{
  "name": "@neftie/subgraph",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ neftielabs/neftie-rinkeby",
    "create-local": "graph create --node http://localhost:8020/ neftielabs/neftie-rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ neftielabs/neftie-rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neftielabs/neftie-rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
